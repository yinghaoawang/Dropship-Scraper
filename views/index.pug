extends layout

block content
    //for value, key in data.scrape_data
        //if key != 'raw_html'
            //p #{key}: #{value}

    //p #{data.scrape_data.movies.title}
    for movie in data.scrape_data.movies
        li
            span #{movie.title}

    p List:
    ul
    -var index = 1

    for movie in data.scrape_data.list
        li
            span #{index++}. 
            a(href=movie['link']) #{movie['rating']} - #{movie['title']}

    // #{data.scrape_data['raw_html']}

